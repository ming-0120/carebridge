# CareBridge 프로젝트 - 작업 중 발생한 오류 정리 (발생 시간순)

## 개요
관리자 패널(admin_panel) 작업 중 발생했던 주요 오류들을 **발생 시간순**으로 정리한 문서입니다.

**주요 수정 기간**: 2025년 12월 1일 ~ 12월 15일
- **2025-12-12**: admin 페이지 버그 수정 (페이지네이션, AJAX 요청 처리 등)
- **2025-12-10**: admin 수정 및 QNA 업무 시작
- **2025-12-09**: admin 페이지 CSS 수정
- **2025-12-08**: accounts 관련 수정
- **2025-12-05**: admin 주석 작업
- **2025-12-04**: admin 관련 CSS 수정
- **2025-12-03**: 관리자 CSS 세부 수정
- **2025-12-02**: 관리자 CSS 수정, admin 브랜치 머지
- **2025-12-01**: 관리자 모드 시작

---

## 📅 발생 시간순 정리

### 🔴 1단계: 초기 개발 단계 (기본 기능 구현)

#### 1-1. 면허번호 검증 로직 문제
**발생 시기**: 승인대기 페이지 초기 개발 단계

### 문제 상황
- **증상**: 면허번호와 주민번호 뒷자리를 비교하는 로직이 복잡하고 오류 발생 가능성이 높음
- **발생 위치**: `apps/admin_panel/views.py` - `approval_pending` 함수

### 해결 방법
```python
# ✅ 올바른 예시: 면허번호와 주민번호 뒷자리 추출 및 비교
# 면허번호 뒷자리 추출 (영어코드$주민번호7자리 형식에서 뒷자리만)
license_back = doctor.license_no[3:] if len(doctor.license_no) > 3 else ''

# 주민번호 뒷자리 추출
resident_reg_no = doctor.user.resident_reg_no
if '-' in resident_reg_no:
    resident_back = resident_reg_no.split('-')[1] if len(resident_reg_no.split('-')) > 1 else ''
else:
    resident_back = resident_reg_no[-7:] if len(resident_reg_no) >= 7 else ''

# 비교
is_valid_license = (license_back == resident_back)
```

### 핵심 원칙
- **안전한 문자열 처리**: 길이 확인 후 슬라이싱
- **다양한 형식 대응**: 하이픈 포함/미포함 모두 처리
- **명확한 변수명 사용**: `license_back`, `resident_back` 등

### 적용 위치
```python:996:1006:apps/admin_panel/views.py
# 면허번호 뒷자리 추출 (영어코드$주민번호7자리 형식에서 뒷자리만)
license_back = doctor.license_no[3:] if len(doctor.license_no) > 3 else ''

# 주민번호 뒷자리 추출
resident_reg_no = doctor.user.resident_reg_no
if '-' in resident_reg_no:
    resident_back = resident_reg_no.split('-')[1] if len(resident_reg_no.split('-')) > 1 else ''
else:
    resident_back = resident_reg_no[-7:] if len(resident_reg_no) >= 7 else ''

is_valid_license = (license_back == resident_back)
```

---

#### 1-2. 페이지네이션 후 Python 정렬 함수 사용 문제
**발생 시기**: 목록 페이지 개발 단계 (사용자목록, 의사목록 등)

### 문제 상황
- **증상**: 페이지네이션 후 Python `sorted()` 함수를 사용하여 정렬하면 페이지 내부만 재정렬되어 전체 정렬이 깨짐
- **발생 위치**: `apps/admin_panel/views.py` - 모든 목록 페이지 (사용자목록, 의사목록, 병원목록, 승인대기, 1:1문의 등)

### 잘못된 코드 예시
```python
# ❌ 잘못된 예시: 페이지네이션 후 정렬
page_obj, total_count = paginate_queryset(request, users, per_page=5)
users_with_number = sorted(
    users_with_number,
    key=lambda x: (not x['is_new_user'], x['user'].created_at),
    reverse=True
)  # 이렇게 하면 페이지 내부만 정렬되어 전체 정렬이 깨짐
```

### 해결 방법
```python
# ✅ 올바른 예시: DB 레벨에서 정렬 후 페이지네이션
users = users.order_by('-created_at')  # DB에서 정렬
page_obj, total_count = paginate_queryset(request, users, per_page=5)
# 페이지네이션 후에는 정렬하지 않고 번호만 계산
users_with_number = []
for idx, user in enumerate(page_obj):
    users_with_number.append({
        'user': user,
        'number': start_index + idx
    })
```

### 핵심 원칙
- **정렬은 반드시 DB 레벨에서 처리** (`order_by()` 사용)
- 페이지네이션 후에는 정렬하지 않고 번호만 계산
- 신규 가입자 표시 등 특별한 표시가 필요한 경우, 정렬 대신 템플릿에서 표시만 처리

### 적용 범위
- 사용자목록 (`user_list`)
- 의사목록 (`doctor_list`)
- 병원목록 (`hospital_list`)
- 승인대기 (`approval_pending`)
- 1:1문의 (`qna_list`)

---

### 🟠 2단계: AJAX 기능 추가 단계

#### 2-1. AJAX 요청과 일반 POST 요청 구분 문제
**발생 시기**: AJAX 기능을 추가한 직후

### 문제 상황
- **증상**: AJAX 요청인 경우에도 리다이렉트를 반환하여 AJAX 요청이 실패함
- **발생 위치**: `apps/admin_panel/views.py` - 승인/거절 처리 (`approval_pending`)

### 잘못된 코드 예시
```python
# ❌ 잘못된 예시: AJAX 요청과 일반 요청을 구분하지 않음
if request.method == 'POST':
    # 승인/거절 처리
    if action == 'approve' or action == 'reject':
        # 처리 로직...
        pass
    
    # 항상 리다이렉트 (AJAX 요청도 리다이렉트됨)
    return redirect('approval_pending')
```

### 해결 방법
```python
# ✅ 올바른 예시: AJAX 요청과 일반 요청 구분
if request.method == 'POST':
    # 처리 로직...
    if action == 'approve' or action == 'reject':
        # 승인/거절 처리
        pass
    
    # AJAX 요청이 아닌 경우에만 리다이렉트
    if request.headers.get('X-Requested-With') != 'XMLHttpRequest':
        return redirect('approval_pending')
```

### 핵심 원칙
- **AJAX 요청인지 확인**: `request.headers.get('X-Requested-With') == 'XMLHttpRequest'`
- AJAX 요청에는 리다이렉트를 반환하지 않도록 처리
- 일반 POST 요청에만 리다이렉트 반환

### 적용 위치
```python:1091:1094:apps/admin_panel/views.py
# 승인/거절 처리 후 리다이렉트 (AJAX 요청이 아닌 경우에만)
# 페이지네이션 AJAX 요청은 리다이렉트하지 않음
if request.headers.get('X-Requested-With') != 'XMLHttpRequest':
    return redirect('approval_pending')
```

#### 2-2. 페이지네이션 AJAX 요청 처리 문제
**발생 시기**: AJAX 페이지네이션 기능 추가 후

### 문제 상황
- **증상**: 페이지네이션 AJAX 요청 시 상세 정보 ID 파라미터(`doctor_id`, `user_id`, `hospital_id`)가 포함되어 서버가 상세 정보를 반환하려고 시도함
- **발생 위치**: `apps/admin_panel/static/admin_panel/js/admin_common.js` - `handlePaginationAjax` 함수

### 문제 원인
- 페이지네이션 AJAX 요청은 목록 조회용이므로 상세 정보 ID 파라미터가 필요 없음
- 하지만 URL에 상세 정보 ID가 포함되어 있으면 서버가 상세 정보를 반환하려고 시도함

### 해결 방법
- 페이지네이션 AJAX 요청 시 상세 정보 ID 파라미터를 제거
- 서버가 목록 뷰를 정상적으로 반환하도록 보장

### 핵심 원칙
- **페이지네이션 AJAX 요청은 목록 조회용**이므로 상세 정보 ID 파라미터 제거
- 서버가 목록 뷰를 정상적으로 반환하도록 보장

---

### 🟡 3단계: 인터랙티브 기능 추가 단계

#### 3-1. 이벤트 리스너 중복 연결 문제
**발생 시기**: 체크박스, 버튼 등 인터랙티브 기능 추가 후

### 문제 상황
- **증상**: DOM 업데이트 후 이벤트 리스너를 다시 연결할 때 중복 연결되어 이벤트가 여러 번 발생함
- **발생 위치**: 모든 JavaScript 파일 (예: `approval_pending.js`, `qna_list.js` 등)

### 해결 방법
```javascript
// ✅ 올바른 예시: 기존 이벤트 리스너 제거 후 새로 추가
const checkbox = document.getElementById('selectAll');

// 기존 이벤트 리스너 제거
if (checkbox._changeHandler) {
    checkbox.removeEventListener('change', checkbox._changeHandler);
}

// 새로운 이벤트 핸들러 생성 및 저장
checkbox._changeHandler = function() {
    // 처리 로직...
};

// 이벤트 리스너 추가
checkbox.addEventListener('change', checkbox._changeHandler);
```

### 핵심 원칙
- **기존 이벤트 리스너를 제거한 후 새로 추가**
- 이벤트 핸들러를 요소에 저장 (`_changeHandler`, `_clickHandler` 등)하여 나중에 제거 가능하도록 함
- DOM 업데이트 후 이벤트 리스너를 다시 연결할 때 반드시 적용

### 적용 위치
- 체크박스 이벤트 리스너 (`attachCheckboxListeners`)
- 버튼 이벤트 리스너 (`attachButtonListeners`)
- 정렬 링크 이벤트 리스너 (`attachSortListeners`)
- 테이블 행 클릭 이벤트 리스너 (`attachTableRowListeners`)

#### 3-2. 체크박스 셀 클릭 시 이벤트 전파 문제
**발생 시기**: 체크박스와 행 클릭 기능을 동시에 구현한 후

### 문제 상황
- **증상**: 체크박스 셀 클릭 시 행 클릭 이벤트도 발생하여 체크박스 선택/해제와 행 클릭 기능이 충돌함
- **발생 위치**: `apps/admin_panel/static/admin_panel/js/approval_pending.js` - `attachCheckboxListeners` 함수

### 해결 방법
```javascript
// ✅ 올바른 예시: 체크박스 셀 클릭 시 이벤트 전파 방지
const checkboxCells = document.querySelectorAll('.checkbox-cell');

checkboxCells.forEach(cell => {
    cell.addEventListener('click', function(e) {
        e.stopPropagation();  // 이벤트 전파 방지
    });
});
```

### 핵심 원칙
- **체크박스 셀 클릭 시 이벤트 전파 방지**: `e.stopPropagation()`
- 체크박스 선택/해제 기능과 행 클릭 기능이 충돌하지 않도록 함

### 적용 위치
```javascript:828:868:apps/admin_panel/static/admin_panel/js/approval_pending.js
// ========= 체크박스 셀 클릭 시 이벤트 전파 방지 =========
// 목적: 체크박스 셀 클릭 시 이벤트가 행으로 전파되지 않도록 방지
const checkboxCells = document.querySelectorAll('.checkbox-cell');

checkboxCells.forEach(cell => {
    cell.addEventListener('click', function(e) {
        e.stopPropagation();  // 이벤트 전파 방지
    });
});
```

#### 3-3. 페이지네이션 후 이벤트 리스너 재연결 문제
**발생 시기**: AJAX 페이지네이션 기능이 완성된 후

### 문제 상황
- **증상**: 페이지네이션 후 새로 추가된 DOM 요소에 이벤트 리스너가 연결되지 않음
- **발생 위치**: 모든 목록 페이지 JavaScript 파일

### 해결 방법
```javascript
// ✅ 올바른 예시: 페이지네이션 후 이벤트 리스너 재연결 함수 정의
window.reattachTableRowListeners = function() {
    // 테이블 행 클릭 이벤트 리스너 재연결
    attachTableRowListeners('.approval-row[data-doctor-id]', 'data-doctor-id', selectDoctor);
    // 체크박스 이벤트 리스너 재연결
    attachCheckboxListeners();
    // 버튼 이벤트 리스너 재연결
    attachButtonListeners();
    // 정렬 링크 이벤트 리스너 재연결
    attachSortListeners();
};

// handlePaginationAjax에서 호출
if (typeof window.reattachTableRowListeners === 'function') {
    window.reattachTableRowListeners();
}
```

### 핵심 원칙
- **페이지네이션 후 모든 이벤트 리스너를 재연결**
- `window` 객체에 함수를 저장하여 `admin_common.js`에서 호출 가능하도록 함
- 중복 호출 방지 로직 포함

### 적용 위치
```javascript:1120:1130:apps/admin_panel/static/admin_panel/js/approval_pending.js
window.reattachTableRowListeners = function() {
    // 중복 호출 방지: attachPaginationListeners는 admin_common.js의 handlePaginationAjax에서 이미 호출됨
    // 그 다음 테이블 행 클릭 이벤트 리스너 재연결
    attachTableRowListeners('.approval-row[data-doctor-id]', 'data-doctor-id', selectDoctor);
    // 체크박스 이벤트 리스너 재연결
    attachCheckboxListeners();
    // 버튼 이벤트 리스너 재연결
    attachButtonListeners();
    // 정렬 링크 이벤트 리스너 재연결
    attachSortListeners();
};
```

---

### 🟢 4단계: UI/UX 개선 단계

#### 4-1. 페이지네이션 링크 선택자 문제
**발생 시기**: 다양한 HTML 구조를 지원하려고 할 때

### 문제 상황
- **증상**: 페이지네이션 링크를 찾지 못하여 AJAX 페이지네이션이 작동하지 않음
- **발생 위치**: `apps/admin_panel/static/admin_panel/js/admin_common.js` - `attachPaginationListeners` 함수

### 해결 방법
```javascript
// ✅ 올바른 예시: 여러 선택자를 통합하여 페이지네이션 링크 찾기
const paginationLinks = document.querySelectorAll(
    '.pagination a.page-link[href], ' +           // 기존 HTML 링크
    '.pagination a.pagination-link[href], ' +     // 로그에서 확인된 링크
    '.pagination a[href], ' +                      // 안전을 위해 모든 href 속성을 가진 <a> 태그
    'nav[aria-label="Page navigation"] a.page-link[href], ' +  // nav 내부 .page-link
    'nav[aria-label="Page navigation"] a.pagination-link[href], ' +  // nav 내부 .pagination-link
    'nav[aria-label="Page navigation"] a[href]'    // nav 내부 모든 href 속성을 가진 <a> 태그
);
```

### 핵심 원칙
- **여러 선택자를 통합하여 페이지네이션 링크 찾기**
- 다양한 HTML 구조에 대응할 수 있도록 여러 선택자 사용
- 디버깅 로그 추가하여 문제 발생 시 원인 파악 용이

### 적용 위치
```javascript:1655:1662:apps/admin_panel/static/admin_panel/js/admin_common.js
const paginationLinks = document.querySelectorAll(
    '.pagination a.page-link[href], ' +
    '.pagination a.pagination-link[href], ' +
    '.pagination a[href], ' +
    'nav[aria-label="Page navigation"] a.page-link[href], ' +
    'nav[aria-label="Page navigation"] a.pagination-link[href], ' +
    'nav[aria-label="Page navigation"] a[href]'
);
```

#### 4-2. 승인대기 페이지 상세 정보 표시 문제
**발생 시기**: 상세 정보 표시 기능을 추가한 후

### 문제 상황
- **증상**: 기본 상태에서도 상세 정보 섹션이 표시되어 레이아웃이 깨짐
- **발생 위치**: `apps/admin_panel/static/admin_panel/js/approval_pending.js` - DOMContentLoaded 이벤트

### 해결 방법
```javascript
// ✅ 올바른 예시: URL에 doctor_id가 없으면 상세정보 섹션 숨기기
const selectedDoctorId = urlParams.get('doctor_id');

if (!selectedDoctorId) {
    const detailSection = document.querySelector('.user-detail-section');
    if (detailSection) {
        detailSection.remove();  // 상세정보 섹션 제거
    }
}
```

### 핵심 원칙
- **기본 상태에서는 상세정보 섹션을 완전히 숨김**
- URL에 `doctor_id` 파라미터가 있을 때만 상세정보 표시

### 적용 위치
```javascript:1186:1195:apps/admin_panel/static/admin_panel/js/approval_pending.js
// ========= 기본 상태에서 상세정보 섹션 숨기기 =========
// 목적: URL에 doctor_id가 없으면 상세정보 섹션을 완전히 숨김
if (!selectedDoctorId) {
    const detailSection = document.querySelector('.user-detail-section');
    if (detailSection) {
        detailSection.remove();
    }
}
```

---

### 🔵 5단계: 경험을 통한 학습 단계

#### 5-1. 디버깅 접근 방법 문제
**발생 시기**: 여러 오류를 경험한 후 패턴을 파악한 단계

### 문제 상황
- **증상**: 클라이언트 사이드 문제로 보여도 실제로는 서버 사이드 로직 문제인 경우가 많음
- **발생 위치**: 모든 기능 개발 시

### 잘못된 접근 방법
- ❌ 증상만 보고 클라이언트 사이드 문제로 가정
- ❌ JavaScript 수정만으로 해결하려고 시도
- ❌ 서버 뷰 함수의 POST 처리 로직을 늦게 확인

### 올바른 접근 방법
- ✅ **서버 사이드 로직 우선 확인**
  - POST 요청 문제는 `views.py`의 해당 뷰 함수부터 확인
  - AJAX 요청과 일반 요청을 구분하는 로직 확인
  - 리다이렉트가 AJAX 요청을 방해하지 않는지 확인
- ✅ **전체 흐름 확인**
  - 클라이언트 → 서버 → 응답 전체를 함께 확인
  - 네트워크 탭에서 요청뿐 아니라 응답 상태/본문도 확인
  - 서버 뷰 함수의 흐름을 추적

### 핵심 원칙
- **서버 사이드 로직부터 확인**
- **전체 흐름을 함께 확인**
- **AJAX 요청과 일반 요청을 명확히 구분**


---

---

## 📊 발생 시간순 요약

| 단계 | 오류 번호 | 오류명 | 발생 시기 | 수정 날짜 |
|------|----------|--------|----------|----------|
| 1단계 | 1-1 | 면허번호 검증 로직 문제 | 승인대기 페이지 초기 개발 | 2025-12-01~12-05 |
| 1단계 | 1-2 | 페이지네이션 후 Python 정렬 함수 사용 문제 | 목록 페이지 개발 | 2025-12-02~12-05 |
| 2단계 | 2-1 | AJAX 요청과 일반 POST 요청 구분 문제 | AJAX 기능 추가 직후 | 2025-12-08~12-10 |
| 2단계 | 2-2 | 페이지네이션 AJAX 요청 처리 문제 | AJAX 페이지네이션 추가 후 | 2025-12-10~12-12 |
| 3단계 | 3-1 | 이벤트 리스너 중복 연결 문제 | 인터랙티브 기능 추가 후 | 2025-12-09~12-10 |
| 3단계 | 3-2 | 체크박스 셀 클릭 시 이벤트 전파 문제 | 체크박스와 행 클릭 동시 구현 후 | 2025-12-10~12-12 |
| 3단계 | 3-3 | 페이지네이션 후 이벤트 리스너 재연결 문제 | AJAX 페이지네이션 완성 후 | 2025-12-12 |
| 4단계 | 4-1 | 페이지네이션 링크 선택자 문제 | 다양한 HTML 구조 지원 시도 | 2025-12-12 |
| 4단계 | 4-2 | 승인대기 페이지 상세 정보 표시 문제 | 상세 정보 표시 기능 추가 후 | 2025-12-10~12-12 |
| 5단계 | 5-1 | 디버깅 접근 방법 문제 | 여러 오류 경험 후 패턴 파악 | 2025-12-12 이후 |

---

## 요약 및 권장사항

### 핵심 원칙
1. **페이지네이션 후 Python `sorted()` 사용 금지** - DB 레벨에서 정렬
2. **AJAX 요청과 일반 요청 구분 필수** - `X-Requested-With` 헤더 확인
3. **이벤트 리스너 중복 방지** - 기존 리스너 제거 후 새로 추가
4. **서버 사이드 로직 우선 확인** - 클라이언트 문제로 보여도 서버부터 확인
5. **페이지네이션 후 이벤트 리스너 재연결** - DOM 업데이트 후 필수

### 디버깅 체크리스트
- [ ] 서버 뷰 함수의 POST 처리 로직 확인
- [ ] AJAX 요청과 일반 요청 구분 확인
- [ ] 페이지네이션 후 정렬 로직 확인
- [ ] 이벤트 리스너 중복 연결 확인
- [ ] 네트워크 탭에서 요청/응답 확인
- [ ] 브라우저 콘솔에서 에러 확인

### 예방 방법
- 코드 리뷰 시 위의 핵심 원칙 확인
- 테스트 시 페이지네이션, AJAX 요청, 이벤트 리스너 동작 확인
- 공통 함수 사용으로 일관성 유지 (`admin_common.js` 활용)

---

## 참고 파일
- `apps/admin_panel/views.py` - 서버 사이드 로직
- `apps/admin_panel/static/admin_panel/js/admin_common.js` - 공통 JavaScript 함수
- `apps/admin_panel/static/admin_panel/js/approval_pending.js` - 승인대기 페이지 JavaScript
- `apps/admin_panel/static/admin_panel/js/qna_list.js` - 1:1문의 페이지 JavaScript

---

**작성일**: 2025년 12월
**최종 수정일**: 2025년 12월 15일
**주요 수정 기간**: 2025년 12월 1일 ~ 12월 15일
**작성자**: Cursor AI
**버전**: 1.1

